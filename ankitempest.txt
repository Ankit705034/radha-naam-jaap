* { box-sizing: border-box; }

:root {
  --ink: #3a1727;
  --muted: #7c5a63;
  --rose: #9d3155;
  --deep: #5d1934;
  --pink: #f3c9d0;
  --cream: #fffaf7;
  --line: #efdfe0;
  --gold: #d7a35b;
  --soft: #f7e7e1;
  --sage: #4e8d6d;
  --shadow: rgba(92, 34, 56, 0.18);
}

html {
  background: linear-gradient(180deg, #f9e7de 0%, #efd7d0 100%);
}

body {
  margin: 0;
  min-width: 320px;
  min-height: 100vh;
  display: grid;
  place-items: center;
  color: var(--ink);
  background:
    radial-gradient(circle at top left, rgba(255,255,255,0.8), transparent 28%),
    radial-gradient(circle at bottom right, rgba(160,67,102,0.14), transparent 30%),
    linear-gradient(135deg, #f8eadf 0%, #f0d9d8 48%, #e8d3d7 100%);
  font-family: 'Noto Sans Devanagari', sans-serif;
}

button, input {
  font: inherit;
}

button {
  cursor: pointer;
}

img {
  display: block;
  max-width: 100%;
}

.app-shell {
  position: relative;
  width: min(100%, 440px);
  min-height: 100vh;
  margin: 0 auto;
  padding: 18px 18px 26px;
  background: linear-gradient(180deg, rgba(255,255,255,0.72), rgba(255,255,255,0.58));
  border: 1px solid rgba(255,255,255,0.6);
  box-shadow: 0 28px 60px rgba(72, 33, 44, 0.14);
  backdrop-filter: blur(12px);
  overflow: hidden;
}

.app-shell::before {
  content: '';
  position: absolute;
  inset: 0 auto auto 50%;
  width: 180%;
  height: 260px;
  transform: translateX(-50%);
  background: radial-gradient(circle at 50% 0%, rgba(244, 182, 117, 0.32), transparent 52%);
  pointer-events: none;
}

main {
  position: relative;
  z-index: 1;
}

.topbar {
  position: relative;
  z-index: 1;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 6px 4px 16px;
}

.brand-lockup,
.header-actions,
.stat-heading,
.target-line,
.section-heading,
.history-summary,
.cloud-banner,
.manual-entry,
.manual-form {
  display: flex;
  align-items: center;
}

.brand-lockup {
  gap: 12px;
}

.brand-mark {
  width: 42px;
  height: 42px;
  display: grid;
  place-items: center;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--deep), #7d2449);
  color: #ffe6b8;
  font-family: 'Yatra One';
  font-size: 12px;
  box-shadow: 0 10px 20px rgba(91, 29, 52, 0.25);
}

.eyebrow,
.section-kicker {
  margin: 0;
  color: var(--rose);
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 1.4px;
  text-transform: uppercase;
}

.brand-lockup h1 {
  margin: 1px 0 0;
  font-family: 'Yatra One';
  font-size: 23px;
  font-weight: 400;
  line-height: 1;
  color: var(--deep);
}

.header-actions {
  gap: 8px;
}

.icon-btn,
.close-btn {
  width: 38px;
  height: 38px;
  border: 1px solid rgba(124, 90, 99, 0.18);
  border-radius: 50%;
  background: rgba(255,255,255,0.8);
  color: var(--deep);
  font-size: 18px;
  box-shadow: 0 8px 16px rgba(103, 55, 75, 0.08);
}

.icon-btn:hover,
.close-btn:hover {
  background: #fff;
}

.hero-panel {
  position: relative;
  display: flex;
  align-items: center;
  justify-content: space-between;
  min-height: 240px;
  padding: 26px 18px 18px;
  overflow: hidden;
  border-radius: 26px;
  background:
    linear-gradient(135deg, rgba(31, 14, 23, 0.18), rgba(146, 52, 83, 0.36)),
    url('barsana.jpg') center/cover no-repeat;
  box-shadow: 0 22px 35px rgba(109, 57, 71, 0.22);
}

.hero-panel::after {
  content: '';
  position: absolute;
  width: 240px;
  height: 240px;
  right: -62px;
  top: -92px;
  border-radius: 50%;
  border: 1px solid rgba(255,255,255,0.22);
  box-shadow: 0 0 0 26px rgba(255,255,255,0.06), 0 0 0 52px rgba(255,255,255,0.03);
}

.hero-copy {
  position: relative;
  z-index: 1;
  max-width: 220px;
}

.date-chip {
  display: inline-block;
  padding: 6px 12px;
  border: 1px solid rgba(255,255,255,0.5);
  border-radius: 30px;
  background: rgba(255,255,255,0.08);
  color: #ffe9cb;
  font-size: 12px;
  font-weight: 700;
}

.hero-copy h2 {
  margin: 16px 0 8px;
  font-family: 'Yatra One';
  font-size: clamp(28px, 7vw, 40px);
  font-weight: 400;
  line-height: 1.2;
  color: #fff6fb;
}

.hero-copy h2 em {
  color: #f7d596;
  font-style: normal;
}

.hero-copy p {
  margin: 0;
  color: rgba(255,255,255,0.87);
  font-size: 12px;
  line-height: 1.6;
}

.lotus-seal {
  position: relative;
  z-index: 1;
  display: grid;
  place-items: center;
  width: 110px;
  height: 110px;
  border-radius: 50%;
  overflow: hidden;
  border: 2px solid rgba(255,255,255,0.42);
  background: rgba(255,255,255,0.14);
  box-shadow: 0 16px 30px rgba(42, 16, 21, 0.25);
  transform: rotate(-6deg);
}

.lotus-seal img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
}

.lotus-seal span {
  position: absolute;
  bottom: 8px;
  left: 50%;
  transform: translateX(-50%) rotate(6deg);
  font-family: 'Yatra One';
  font-size: 16px;
  color: #fff7ed;
  text-shadow: 0 3px 8px rgba(93, 25, 52, 0.7);
}

.stats-grid {
  display: grid;
  grid-template-columns: 1.35fr 1fr 1fr;
  gap: 12px;
  margin: 18px 0 28px;
}

.stat-card {
  min-height: 128px;
  padding: 16px 16px 14px;
  background: rgba(255,255,255,0.68);
  border: 1px solid rgba(255,255,255,0.72);
  border-radius: 18px;
  box-shadow: 0 12px 28px rgba(109, 57, 71, 0.08);
}

.stat-card.featured {
  background: linear-gradient(180deg, rgba(255,255,255,0.9), rgba(255,245,247,0.82));
}

.stat-heading,
.target-line,
.history-summary,
.cloud-banner,
.section-heading {
  justify-content: space-between;
}

.stat-heading {
  color: var(--muted);
  font-size: 11px;
  font-weight: 700;
}

.status-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  background: #47a76c;
  box-shadow: 0 0 0 4px rgba(71,167,108,0.12);
}

.stat-card strong {
  display: block;
  margin: 8px 0 2px;
  color: var(--deep);
  font-family: 'Yatra One';
  font-size: 34px;
  line-height: 1.05;
  font-weight: 400;
}

.stat-label,
.stat-foot,
.target-line,
.history-summary,
.cloud-banner p,
.dialog-card p {
  color: var(--muted);
}

.stat-label,
.stat-foot {
  display: block;
  font-size: 11px;
}

.stat-foot {
  color: #a07d81;
}

.target-line {
  display: flex;
  align-items: center;
  font-size: 11px;
  color: var(--muted);
}

.progress-track {
  height: 8px;
  margin-top: 8px;
  border-radius: 999px;
  background: rgba(151, 87, 103, 0.12);
  overflow: hidden;
}

.progress-track span {
  display: block;
  height: 100%;
  width: 0;
  border-radius: inherit;
  background: linear-gradient(90deg, #e0a466, #c84f6f 65%, #7d2449);
  transition: width 0.35s ease;
}

.practice-layout,
.history-section {
  max-width: 780px;
  margin: 0 auto 26px;
}

.section-heading {
  display: flex;
  align-items: center;
  margin-bottom: 15px;
}

.section-heading h2 {
  margin: 4px 0 0;
  color: var(--deep);
  font-size: 22px;
}

.save-note {
  padding: 7px 10px;
  border-radius: 999px;
  background: rgba(78, 141, 109, 0.1);
  color: var(--sage);
  font-size: 11px;
  font-weight: 700;
}

.radhe-btn {
  display: grid;
  place-items: center;
  width: min(100%, 310px);
  aspect-ratio: 1;
  margin: 0 auto 22px;
  border: 12px solid #f8d8b5;
  border-radius: 50%;
  background: radial-gradient(circle at 35% 25%, #ec7d8f, #ad2c59 52%, #5e1a38 78%);
  color: #fef3d0;
  font-family: 'Yatra One';
  font-size: clamp(42px, 8vw, 70px);
  letter-spacing: 1px;
  box-shadow: 0 0 0 6px rgba(153,66,93,0.12), 0 20px 0 #d7a39a, 0 22px 34px rgba(76, 31, 45, 0.2);
  transition: transform 0.15s ease, box-shadow 0.15s ease;
  touch-action: manipulation;
  user-select: none;
  -webkit-tap-highlight-color: transparent;
  -webkit-appearance: none;
  appearance: none;
  outline: none;
}

.radhe-btn:focus-visible {
  outline: 3px solid #fff;
  outline-offset: 8px;
}

.radhe-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 0 0 7px rgba(153,66,93,0.15), 0 24px 0 #d7a39a, 0 28px 42px rgba(76, 31, 45, 0.24);
}

.radhe-btn:active {
  transform: translateY(8px);
  box-shadow: 0 0 0 6px rgba(153,66,93,0.1), 0 7px 0 #d7a39a, 0 12px 18px rgba(76, 31, 45, 0.16);
}

.manual-entry {
  gap: 12px;
  margin-top: 12px;
  padding: 14px 14px 12px;
  border-radius: 18px;
  background: rgba(255,255,255,0.78);
  border: 1px solid rgba(178, 130, 140, 0.2);
  box-shadow: 0 8px 18px rgba(116, 71, 79, 0.05);
}

.manual-icon {
  display: grid;
  place-items: center;
  width: 42px;
  height: 42px;
  border-radius: 12px;
  background: linear-gradient(135deg, #f8d7ba, #f0af8d);
  color: var(--deep);
  font-weight: 800;
}

.manual-copy {
  flex: 1;
}

.manual-copy strong {
  display: block;
  color: var(--ink);
  font-size: 14px;
  margin-bottom: 2px;
}

.manual-copy span {
  display: block;
  color: var(--muted);
  font-size: 11px;
}

.manual-form {
  gap: 8px;
  margin-top: 8px;
  width: 100%;
}

.manual-form input[type="date"],
.manual-form input[type="number"],
.dialog-card input {
  width: 100%;
  min-height: 42px;
  padding: 10px 12px;
  border: 1px solid rgba(124, 90, 99, 0.18);
  border-radius: 12px;
  background: rgba(255,255,255,0.82);
  color: var(--ink);
}

.manual-form input[type="date"] {
  width: 145px;
}

.manual-form input[type="number"] {
  flex: 1;
}

.add-btn,
.dialog-submit,
.text-btn {
  min-height: 42px;
  border: 0;
  border-radius: 12px;
  padding: 10px 16px;
  font-weight: 700;
}

.add-btn,
.dialog-submit {
  background: linear-gradient(135deg, var(--deep), #7a2347);
  color: #fff;
  box-shadow: 0 10px 18px rgba(93, 25, 52, 0.18);
}

.text-btn {
  background: transparent;
  color: var(--rose);
  border: 1px solid rgba(157, 49, 85, 0.24);
}

.history-summary {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 12px 14px;
  background: rgba(255,255,255,0.72);
  border: 1px solid rgba(255,255,255,0.75);
  border-radius: 16px;
  color: var(--muted);
  font-size: 12px;
}

.history-summary strong {
  color: var(--deep);
  font-size: 20px;
  font-family: 'Yatra One';
  font-weight: 400;
}

.history-divider {
  width: 1px;
  height: 18px;
  background: rgba(122, 74, 83, 0.25);
}

.history-list {
  margin-top: 14px;
}

.history-row {
  display: grid;
  grid-template-columns: 84px 1fr 60px;
  gap: 10px;
  align-items: center;
  padding: 12px 10px;
  background: rgba(255,255,255,0.7);
  border: 1px solid rgba(255,255,255,0.7);
  border-radius: 14px;
  margin-bottom: 10px;
}

.day {
  color: var(--ink);
  font-size: 12px;
  line-height: 1.3;
  font-weight: 700;
}

.day small {
  color: var(--muted);
  font-size: 10px;
  text-transform: uppercase;
}

.history-bar {
  position: relative;
  height: 12px;
  border-radius: 999px;
  background: rgba(151, 87, 103, 0.1);
  overflow: hidden;
}

.history-bar span {
  position: absolute;
  inset: 0 auto 0 0;
  border-radius: inherit;
  background: linear-gradient(90deg, #f2baa1, #d7677a 55%, #8c2649);
}

.history-row strong {
  color: var(--deep);
  font-family: 'Yatra One';
  font-size: 15px;
  font-weight: 400;
  text-align: right;
}

.empty-history {
  padding: 18px 10px;
  border-radius: 14px;
  background: rgba(255,255,255,0.58);
  color: var(--muted);
  text-align: center;
  border: 1px dashed rgba(124, 90, 99, 0.2);
}

.cloud-banner {
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 12px;
  padding: 15px 16px;
  border-radius: 18px;
  background: linear-gradient(135deg, rgba(255,255,255,0.8), rgba(248, 234, 232, 0.75));
  border: 1px solid rgba(179, 136, 145, 0.18);
}

.cloud-icon {
  display: grid;
  place-items: center;
  width: 42px;
  height: 42px;
  border-radius: 14px;
  background: linear-gradient(135deg, #f9d9cc, #f1b3a7);
  color: var(--deep);
  font-size: 22px;
}

.cloud-banner strong {
  display: block;
  margin-bottom: 3px;
  color: var(--deep);
  font-size: 13px;
}

.cloud-banner p {
  margin: 0;
  font-size: 11px;
  line-height: 1.5;
}

.backup-actions {
  display: flex;
  align-items: center;
  gap: 8px;
}

footer {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 8px;
  padding-top: 18px;
  color: var(--muted);
  font-size: 11px;
}

.toast {
  position: fixed;
  left: 50%;
  bottom: 24px;
  transform: translateX(-50%) translateY(18px);
  min-width: 170px;
  max-width: 80vw;
  padding: 10px 14px;
  border-radius: 999px;
  background: rgba(61, 21, 34, 0.92);
  color: #fff;
  text-align: center;
  font-size: 12px;
  opacity: 0;
  transition: opacity 0.25s ease, transform 0.25s ease;
  pointer-events: none;
  z-index: 5;
}

.toast.show {
  opacity: 1;
  transform: translateX(-50%) translateY(0);
}

.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  border: 0;
}

dialog {
  border: 0;
  padding: 0;
  background: transparent;
}

.dialog-card {
  position: relative;
  width: min(90vw, 380px);
  padding: 28px 22px 20px;
  border-radius: 24px;
  background: linear-gradient(180deg, #fffaf7, #f4e4e1);
  border: 1px solid rgba(151, 87, 103, 0.18);
  box-shadow: 0 24px 60px rgba(61, 21, 34, 0.2);
}

.dialog-card::backdrop {
  background: rgba(42, 15, 26, 0.56);
}

.dialog-card h2 {
  margin: 6px 0 10px;
  color: var(--deep);
  font-size: 23px;
}

.dialog-card p {
  margin: 0 0 16px;
  font-size: 12px;
  line-height: 1.6;
}

.dialog-card label {
  display: block;
  margin-bottom: 7px;
  color: var(--ink);
  font-size: 12px;
  font-weight: 700;
}

.dialog-card input {
  margin-bottom: 14px;
}

.close-btn {
  position: absolute;
  right: 14px;
  top: 14px;
  width: 30px;
  height: 30px;
  border: 0;
  background: transparent;
  box-shadow: none;
  font-size: 22px;
  line-height: 1;
}

.dialog-submit {
  width: 100%;
  margin-top: 4px;
}

#resetDataBtn {
  width: 100%;
  margin-top: 10px;
  justify-content: center;
}

@media (max-width: 640px) {
  .app-shell {
    width: min(100%, 430px);
    padding-left: 16px;
    padding-right: 16px;
  }

  .topbar {
    padding-top: 2px;
  }

  .hero-panel {
    min-height: 220px;
    padding: 20px 16px 16px;
  }

  .lotus-seal {
    width: 92px;
    height: 92px;
  }

  .stats-grid {
    grid-template-columns: 1fr 1fr;
    gap: 9px;
    margin-bottom: 28px;
  }

  .stat-card {
    min-height: 118px;
    padding: 14px 14px 12px;
  }

  .stat-card.featured {
    grid-column: span 2;
  }

  .stat-card strong {
    font-size: 28px;
  }

  .manual-entry {
    align-items: flex-start;
    flex-wrap: wrap;
  }

  .manual-copy {
    min-width: calc(100% - 56px);
  }

  .manual-form {
    width: 100%;
    margin-left: 0;
    flex-wrap: wrap;
  }

  .manual-form input[type="date"] {
    width: 100%;
    flex: 1 1 100%;
  }

  .manual-form input[type="number"] {
    flex: 1 1 100%;
  }

  .cloud-banner {
    align-items: flex-start;
    gap: 10px;
    flex-wrap: wrap;
  }

  .backup-actions {
    width: 100%;
    justify-content: flex-end;
  }

  .history-row {
    grid-template-columns: 72px 1fr 52px;
  }
}
